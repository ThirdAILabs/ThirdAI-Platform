'use client'

import { Button } from "@/components/ui/button"

export default function UpdateButton() {
    return <div style={{display: "flex", justifyContent: "center", marginTop: "20px", marginBottom: "20vh"}}>
        <Button>Update model with feedback</Button>
    </div>
}